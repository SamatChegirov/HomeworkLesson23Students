import java.util.ArrayList;

public interface MethodStudents {
    String javaStudents(ArrayList<Student> students);
    String jsStudents(ArrayList<Student> students);
    int yearsStudents(ArrayList<Student> students);
    String nameASymbolStudents(ArrayList<Student> students);
}
